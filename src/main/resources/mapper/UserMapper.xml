<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zun1.mapper.UserMapper">
    <select id="selectUser" resultType="com.zun1.model.User">
        select * from user where id = #{id}
    </select>

    <select id="selectUsers" resultMap="users">
        SELECT *FROM USER
    </select>
    <resultMap id="users" type="com.zun1.model.User">
        <id column="id" property="id" />
        <result column="age" property="age" />
        <result column="name" property="name" />
    </resultMap>
    
    <insert id="insert" parameterType="com.zun1.model.User">
        insert into user (id, age ,name) values
        (#{id}, #{age}, #{name})
    </insert>
</mapper>